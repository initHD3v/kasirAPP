fix: Perbaikan fungsionalitas logout dan penyesuaian router

Memperbaiki:
- Fungsionalitas logout dengan injeksi `AuthRepository` yang benar ke `AuthBloc`.
- Logika redirect `AppRouter` untuk mengarahkan ke halaman login setelah logout.
- Penempatan atribut `android:enableOnBackInvokedCallback` di `AndroidManifest.xml` (meskipun peringatan masih muncul, konfigurasi sudah benar).

Catatan:
- Peringatan `unnecessary_null_comparison` di `products_page.dart` masih belum teratasi (kemungkinan false positive).